# Nordnet Trading Simulator

High-risk, high-reward trading simulator with portfolio tracking and all-in simulator.

## Backend
- FastAPI + Pandas + yfinance
- Run: `uvicorn app.main:app --reload`

## Frontend
- React
- Run: `npm install && npm start`
